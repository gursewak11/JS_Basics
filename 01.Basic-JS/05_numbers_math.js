const num1 = 100;
const num2 = 50;
let val;

// Simple maths with number
val = num1 + num2;
val = num1 * num2;
val = num1 - num2;
val = num1 / num2;
val = num1 % num2;

// Math Object
val = Math.PI;
val = Math.E;
val = Math.round(2.8);
val = Math.round(2.4);
val = Math.floor(2.9);
val = Math.ceil(2.1);
val = Math.abs(-10);
val = Math.pow(8, 2);
val = Math.min(8, 2, 55, 80, 7, 5, 68, 12, 81);
val = Math.max(8, 2, 55, 80, 7, 5, 68, 12, 81);
val = Math.random();

val = Math.floor(Math.random() * 20 + 1);
// Random Number between 1 and 20 as a whole number

// Output
console.log(val);